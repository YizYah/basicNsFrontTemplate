// ns__start_section {{fileInfo}}, loc: propTypes
{{names.component}}.propTypes = {
{{#if (eq boilerPlateInfo.componentType 'creation') }}
  parentId: PropTypes.string,
  refetchQueries: PropTypes.array,
  create{{#if (neq boilerPlateInfo.nodeType nodeTypes.ROOT) }}{{names.singular}}{{/if}}: PropTypes.func,
{{/if}}
{{#if (eq boilerPlateInfo.componentType 'list') }}
{{/if}}
{{#if (eq boilerPlateInfo.componentType 'single') }}
  parentId: PropTypes.string,
  selected: PropTypes.bool,
  updateInstance: PropTypes.func,
  deleteInstance: PropTypes.func,
  refetchQueries: PropTypes.array,
  {{#if (neq boilerPlateInfo.nodeType nodeTypes.ROOT) }}
    onSelect: PropTypes.func,
  {{/if}}
  {{names.singularLowercase}}: PropTypes.shape({
  children: PropTypes.array,
  id: PropTypes.string,
  }),
{{/if}}
// ns__custom_start {{fileInfo}}, loc: addedPropTypes
// ns__custom_end {{fileInfo}}, loc: addedPropTypes
};
// ns__end_section {{fileInfo}}, loc: propTypes

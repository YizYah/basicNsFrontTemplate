

// ns__start_section {{fileInfo}}, loc: compose
{{#if (eq boilerPlateInfo.componentType 'creation') }}
  export default compose(graphql(EXECUTE, { name: 'create{{names.singular}}' }){{> singleChildrenComposeStatements this}})(
  {{names.singular}}CreationForm
  );
{{/if}}
{{#if (eq boilerPlateInfo.componentType 'single') }}
  export default compose(
  graphql(EXECUTE, { name: 'updateInstance' }),
  graphql(EXECUTE, { name: 'deleteInstance' })
  )({{names.singular}});
{{/if}}
// ns__end_section {{fileInfo}}, loc: compose
